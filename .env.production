# Production build config for the static frontend (loaded by Vite during `vite build`).
# Vite always reads .env.production in production mode, independent of Render env
# propagation (which can drop VITE_ vars from the static build). This value points
# the frontend at the separate Node backend.
VITE_API_URL=https://raffle-web-ewbs.onrender.com